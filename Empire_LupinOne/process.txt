	РАЗВЕДКА:
Для начала я просканировал свою сеть. Путем логического исключения выяснил, что ip адрес цели -- 10.0.2.10 
(ipaddress.png).

----------------------------------------------------------------------------------------------------------------------

	СКАНИРОВАНИЕ И ПЕРЕЧИСЛЕНИЕ:
Я запустил сканирование с помощью nmap и узнал следующую информацию (Scans/FirstNmapScan.*):

1. MAC адрес: 08:00:27:52:3D:2C (Oracle VirtualBox virtual NIC);
2. ОС: Linux 4.15 - 5.8;
3. Открытые порты и запушенные сервисы:
	1) 22/tcp ssh OpenSSH 8.4p1 Debian 5 (protocol 2.0):
		Дополнительное сканировование этого порта не дало данных для входа, хато я ущнал алгоритмы шифрования и			ssh ключи.
	2) 80/tcp http Apache httpd 2.4.48 ((Debian))

На порту 80 работает сайт (site.png). Далее я перехожу к поиску уязвимостей и неправильных конфигураций сайта.
При анализе файла robots.txt была найдена скрытая директория /~myfiles. Судя по названию, там хранятся личные файлы.
Перехожу к работе со скрытой директорией. При переходе к скрытой директории я попадаю на страницу "Error 404" 
(hidden1.png). Скорее всего, страница создана для отвлечения внимания хакера (hidden2.png).

Перехожу к анализу исходного кода. В исходном коде никаких важных данных не обнаружил.

перехожу к поиску других директорий и поддоменов.

Сканирование директорий с помощью gobuster дало 3 директории (Scans/FirstGoBusterScanDir.txt):
	/image                (Status: 301) [Size: 306] [--> http://10.0.2.10/image/]
	/manual               (Status: 301) [Size: 307] [--> http://10.0.2.10/manual/]
	/javascript           (Status: 301) [Size: 311] [--> http://10.0.2.10/javascript/]
 
Сканирование с помощью инструмента dirb ничего нового не дало (Scans/FirstDirbScan.txt).

Перехожу к сканированию с помощью инструмента Ffuf. Результат сканирования (Scans/FirstFfufScan.txt):
	Я нашел директорию ~secret, которая содержала подсказку, связвнную с ssh private key (screenShots/secret.png), а также ник icex64 (ВОЗМОЖНО, пользователь!!!).
	Где-то здесь спрятан закрытый ssh, расшифровывать с помощью fasttrack.
	Анализ исходного кода не дал подсказок для поиска этой директории.
	Сканирую с помощью ffuf.
	С 3 попытки после смены (FfufScanDirSecretAttempt3.html) славаря я нашел файл mysecret.txt (mysecret.png).
	Приведенный в файле текст был зашифрован с помощью одного из этих шифров -- cipher.png.
	Дешифрую с помощью CyberShef.Это был base58 (ssh.png). Я сохранил результат в файл id_rsa.
	Далее привожу id_rsa к виду, с которым может работать john:
	ssh2john id_rsa > id_rsa.hash
	Получаю пароль:
	john --wordlist=/usr/share/wordlists/fasttrack.txt id_rsa.hash 
	Результат -- P@55w0rd! (icex64_password.png).

	Так как открыт ssh пытаюсь подключиться по нему, используя icex64 -- P@55w0rd!
	Результат -- данные верны!!! (login.png)

	На машине установлен python3(ОПАСНО!!!)

	В домашней директории пользователя был первый флаг -- 3mp!r3{I_See_That_You_Manage_To_Get_My_Bunny} .
	С помощью python3 я смог скачать флаг:
	На уязвимой машине -- python3 -m http.server , на своей машине -- wget http://10.0.2.10:8000/user.txt. 

----------------------------------------------------------------------------------------------------------------------

	ЭКСПЛУАТАЦИЯ НАЙДЕННЫХ УЯЗВИМОСТЕЙ:
1) Скрытая директория /~myfiles -- неудачно;
2) CVE-2021-40438 -- неудачно.

----------------------------------------------------------------------------------------------------------------------

	ПОВЫШЕНИЕ ПРИВЕЛЕГИЙ:

Анализ истории и файла .bash_history полезных данных не дал.
Версия ядра -- 5.10.0-8-amd64
Данные об ОС: Linux LupinOne 5.10.0-8-amd64 #1 SMP Debian 5.10.46-5 (2021-09-23) x86_64 GNU/Linux
Данные файла /etc/passwd: passwd
Без пароля могу выполнять: /usr/bin/python3.9 /home/arsene/heist.py
SUID бит:
	/usr/lib/openssh/ssh-keysign
	/usr/lib/dbus-1.0/dbus-daemon-launch-helper
	/usr/bin/mount
	/usr/bin/su
	/usr/bin/umount
	/usr/bin/fusermount
	/usr/bin/passwd
	/usr/bin/chsh
	/usr/bin/chfn
	/usr/bin/sudo
	/usr/bin/newgrp
	/usr/bin/gpasswd

Я имею доступ к файлам пользователя arsene. Здесь есть два файла. В файле note.txt arsene указывает, что у него есть секретный файл. Этот сикретный файл -- .secret. Но я не имею к нему доступа. Во втором файле находится python-скрипт. В файл /usr/lib/python3.9/webbrowser.py я дописал строку -- os.system('/bin/bash') для получения обратной оболочки arsene.С помощью команды sudo -u arsene /usr/bin/python3.9 /home/arsene/heist.py получаю обратную оболочку.Теперь я arsene.

/usr/bin/pip -- удинственное, что я могу делать без пароля от имени arsene.
В интернете я нашел возможность повышения привелегий с помощью pip -- https://gtfobins.github.io/gtfobins/pip/#sudo
Используя это, я получил root (root.png).
В домашней директории root был второй флаг -- 3mp!r3{congratulations_you_manage_to_pwn_the_lupin1_box}



ФЛАГИ НАЙДЕНЫ!!!ТЕСТИРОВАНИЕ ОКОНЧЕНО!!!
